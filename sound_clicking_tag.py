from talon import Module

mod = Module()
# this declares a tag in the user namespace (i.e. 'user.sound_click')
mod.tag("sound_clicks", desc="basic commands for working with mouse clicks, but triggered by a sound instead of a key press")